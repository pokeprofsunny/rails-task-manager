<h1><%= @task.title %></h1> <%= link_to "✏️ Edit", edit_task_path(@task) %>


<p>
  <strong>Details:</strong><br>
  <%= @task.details %>
</p>

<p>
  <strong>Completed:</strong>
  <%= @task.completed ? "✅ Yes" : "❌ No" %>
</p>

<%= link_to "← Back to all tasks", tasks_path %>
